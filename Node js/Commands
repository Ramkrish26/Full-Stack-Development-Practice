Node commands:
1. To run using Node
--node <file name>

2. To get the output in a file
--node <file name> >> <output file name>

command prompt
1. To run the command as root user (super user)
--runas/user.adminstrator

2. To list all files in a folder
--dir /s

3. TO create a directory
--mkdir <dir name>

4. To delete a files
--del <filename>

5. To delete a dir along with sub dir
--rmdir /s <dir name>

